{% extends 'base.html.twig' %}

{% block title %}Ideia{% endblock %}

{% block body %}
    <h1>Ideia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ideium.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ ideium.titulo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ideia_index') }}">back to list</a>

    <a href="{{ path('ideia_edit', {'id': ideium.id}) }}">edit</a>

    {{ include('ideia/_delete_form.html.twig') }}
{% endblock %}
